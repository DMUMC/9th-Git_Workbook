## Git & GitHub 핵심 요약

### ## 1. Git이란 무엇인가?

**Git**은 파일의 변경 이력을 체계적으로 관리하는 **분산 버전 관리 시스템(DVCS)**입니다. "최종.hwp", "진짜_최종.hwp"처럼 여러 개의 파일을 만드는 대신, 하나의 파일 안에서 변경된 모든 기록을 추적하고 원하는 시점의 버전으로 쉽게 되돌아갈 수 있게 도와주는 시스템.

-   **파일 변경 사항 추적**: 파일이 언제, 어떻게 수정되었는지 모든 이력을 기록하여 저장 공간을 효율적으로 사용하고 특정 버전으로 쉽게 접근가능.
-   **여러 사용자 간의 작업 조율**: **GitHub**와 같은 원격 저장소(Remote Repository)를 통해 여러 개발자가 동일한 프로젝트의 코드를 공유하고 각자의 컴퓨터(로컬)에서 작업한 뒤, 변경 사항을 손쉽게 병합할 수 있음.
-   **분산 버전 관리 시스템**: 중앙 서버에만 의존하지 않고, 모든 개발자가 프로젝트의 전체 이력을 자신의 컴퓨터에 복사하여 작업. 이를 통해 오프라인 작업이 가능하고, 중앙 저장소에 문제가 생겨도 각자의 로컬 저장소를 통해 복구가능.

---

### ## 2. Git의 작동 방식

Git은 작업을 관리하기 위해 크게 **세 가지 영역**을 사용.

1.  **워킹 디렉토리 (Working Directory)**
    -   현재 내가 **실제로 작업하고 있는 프로젝트 폴더**.
    -   파일의 상태는 **unmodified(변경 없음)**, **modified(변경됨)**로 나뉨.
    -   `.gitignore` 파일을 사용하면 특정 파일이나 폴더(예: 개인 키, 설정 파일)의 변경 사항을 의도적으로 추적하지 않도록 설정할 수 있음.

2.  **스테이징 에어리어 (Staging Area)**
    -   작업한 내용 중 **커밋(버전 기록)에 포함시킬 변경 사항을 준비하는 공간**.
    -   `git add` 명령어를 사용해 `modified` 상태의 파일을 `staged` 상태로 변경하여 이 영역으로 이동.
    -   여러 파일 중 원하는 파일만 선택적으로 준비할 수 있어, 논리적으로 관련된 작업 단위로 커밋을 구성하는 데 유용.

3.  **로컬 저장소 (Local Repository)**
    -   스테이징 에어리어에 준비된 변경 사항들을 **하나의 버전(체크포인트)으로 기록하는 곳**.
    -   `git commit` 명령어를 사용해 `staged` 상태의 파일들을 로컬 저장소에 영구적으로 기록.
    -   이렇게 생성된 커밋들은 각각 고유한 ID(해시)를 가지며, 이력을 통해 관리.
    -   로컬 저장소의 내용을 원격 저장소에 공유하려면 `git push` 명령어를 사용.



---

### ## 3. 브랜치(Branch)와 협업

**브랜치**는 기존 개발 흐름(보통 `main` 브랜치)에 영향을 주지 않고, 독립적인 작업을 수행하기 위해 만드는 **작업 영역**.

-   **독립적인 작업 공간**: 여러 개발자가 동시에 다른 기능을 개발할 때, 각자의 브랜치에서 작업하면 서로의 코드에 영향을 주지 않아 충돌을 최소화.
-   **브랜치 생성 및 전환**:
    -   `git branch [브랜치 이름]`: 새로운 브랜치를 생성.
    -   `git switch [브랜치 이름]`: 해당 브랜치로 작업 공간을 전환.
-   **브랜치 병합 (Merge)**:
    -   `git merge [병합할 브랜치 이름]`: 다른 브랜치에서 완료된 작업 내용을 현재 브랜치로 가져와 병합.
    -   협업 시에는 충돌 위험 때문에 직접 `merge` 하기보다, 아래에서 설명할 **풀 리퀘스트(Pull Request)**를 주로 사용.

---

### ## 4. GitHub를 활용한 협업

**GitHub**는 Git을 사용하는 프로젝트를 위한 웹 기반 호스팅 서비스로, 원격 저장소 역할과 함께 강력한 협업 기능을 제공.

-   **이슈 (Issue)**: 프로젝트에서 해야 할 일, 버그, 기능 개선 등을 **공식적으로 기록하는 공간**. 작업을 시작하기 전 이슈를 생성하여 담당자와 목표를 명확히 해야합.

-   **풀 리퀘스트 (Pull Request, PR)**: 특정 브랜치에서 완료한 작업을 **다른 브랜치(주로 `main`)에 병합해달라고 요청**하는 기능.
    -   PR을 생성하면 다른 팀원들이 변경된 코드를 리뷰하고 피드백을 남길 수 있음. (코드 리뷰)
    -   충분한 검토 후 승인되면, 안전하게 메인 브랜치에 코드를 병합할 수 있어 프로젝트의 품질을 유지하는 데 필수.

---

### ## 5. 필수 Git 명령어

| 명령어 | 설명 |
| :--- | :--- |
| `git clone [URL]` | 원격 저장소(GitHub)의 프로젝트를 내 컴퓨터로 복제. |
| `git status` | 현재 워킹 디렉토리와 스테이징 에어리어의 파일 상태를 확인. |
| `git add [파일 이름]` | 특정 파일의 변경 사항을 스테이징 에어리어에 추가. |
| `git add .` | 현재 디렉토리의 모든 변경 사항을 스테이징 에어리어에 추가. |
| `git commit -m "[커밋 메시지]"` | 스테이징 에어리어의 변경 사항을 로컬 저장소에 기록(커밋). |
| `git log` | 현재까지의 커밋 이력을 시간순으로 조회. |
| `git push origin [브랜치 이름]` | 로컬 저장소의 커밋을 원격 저장소(origin)로 업로드하여 공유. |